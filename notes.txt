CSS Introduction
====================================================
What is css?
----------------------------------
CSS is nothing but it cascadeing stylesheet programming langauge.
It was introduced in the year of 1995, by Hackon Lie.
CSS3 is the combination of CSS + JS.
The first of version of css is called as css level1 in the year of 1996.
After the 2yr that is 1998, next version of css came in market and which was known as CSS Level2.
After 10yrs that is 2008, latest css was introduced which was known as CSS3.


Why we use the css?
-------------------------------------------
As we know, the element of html is ugly in nature.
So, we change the nature of html element we use the css to make them very attractive as well as make the web app responsive in nature.

What are the different types of css are present?
----------------------------------------------------------
As we know there are 3 types of css are present,
a) inline css:
--------------------------
In this type of css we can write the property inside the html element by using the tag called as <style></style>.
Note: Each element in html has the style.


b) internal css:
------------------------------
In this type of css we can write the css property inside the html page, which is located in <head></head> section using the tag called <style></style>.


c) external css:
-------------------------------
In this type css we can write the css property inside the external file which is located in our project root directory.
When write css property inside the external file dont use the <style></style>.
The external file has the extension called as .css 
We link the external css with html page we use the tag called as <link></link>. Inside the link tag we pass some attribute rel means relation with stylesheet and than another attribute called as href which denote the location of css file.

Example:
------------------
<link rel="stylesheet" href="./css/style.css" />

The general syntax of css is as below,

selectors{
    key:value,
    key:value
}



Q. What is mean by selectors in css and explain its types?
----------------------------------------------------------------------------
Def : Selectors is nothing but targetting element.

Types of selectors:
-----------------------------
1. id selectors : It is indicated by # symbol. It is unique.
2. class selector : It is indicated by using .(dot). We can apply multiple classes to single element.
3. tag with class selector
4. tag selector : We can directly target html element like h1, p, div, body, table, ul, li etc ....
5. group selector :  We can create a group also like h1,h2 this one group than h5,h6 another group.
6. universal selector : It is indicated by using * symbol.
7. psudeo classes and psudeo elements


psudeo elements:
------------------------------
1. ::After
2. ::before
3. ::selection
4. ::first-line
5. ::placeholder
6. ::first-letter



Q. How we can declare the variables in css?
-------------------------------------------------------
In css we can declare the variables using the global scope.
In css the global scope declared by using psudeo class :root
The variables are created using --variable_name.
We can acees the variables using the function called as var().

global section indicated by 
:root{
    we define the variables
}
Example:
--------------------
var(--variable_name)


Q. What is mean @import rule?
---------------------------------------------
If i have multiple css files and i want to add these files into a main css file than we can use
@import rule.


How to develop layouts using html and css without using display flex and grid
property?
--------------------------------------------------------------------------------------------
1. width 
2. height / min-height 
3. border 
4. text-align : center, right, left, justify
5. padding
6. margin
7. color / background-color 
8. float : right , left 
9. text-decoration
10. display: block, inline-block
===================================================================================================
border
------------------------------
To make the box attractive.
To know you are in perticular area.

Basic Syntax:
----------------------------
border-width
border-style
border-color 

border : 1px solid black;

-------------------------------------------------------------------
border-image:
---------------------------------------
1. border-image-source : image path --> url()
2. border-image-slice : to combine the image 
3. border-image-width : to set the width of border
4. border-image-repeat : strecth(default)
5. border-image-outset :

==================================================================================

backdrop-fillter:
-----------------------------------
background-color --> rgba
blur()
contrast()
brightness
garyscale
invert

SPA(Single Page Application):
--------------------------------------
display 
-------------------
display: block, inline, inline-block, flex, grid, list, table

What is the difference between inline and inline-block?
----------------------------------------------------------------------
inline : 
---------------
width --> issue
height --> issue
padding 
margin

inline-block
-----------------------
width
height
margin
padding


display:flex 
--------------------------------
1. flex is tool that can be used for to make the divs/section flexible.
2. flex can be work on only one direction at a time, either in row direction or in column direction.
3. by using flex we avoid float property.
4. For learning the flex we know some properties of flex.
two wrods
-----------------------
parent --> flex container
child ---> flex items


1. display: flex --> By default all the divs switched in single row.
2. flex-direction --> by default row, column,row-reverse, column-reverse
3. justify-content --> This property will work in horizantal direction 
                        a) flex-start
                        b) flex-end
                        c) center
                        d) space-between
                        e) space-around
                        f) space-evenly
4. align-items --> This properties will work in vertical direction.
                        a)flex-start
                        b) flex-end
                        c) center
                        d) stretch --> by default 
                    
5. flex-wrap --> by default no-wrap, wrap, wrap-reverse
6. align-content : align-content property will work on multiple rows, while align-items will work in sinle row. 
The following are some properties of align-content.
                        a) flex-start
                        b) flex-end
                        c) center
                        d) space-between
                        e) space-around
                        f) space-evenly

7. order : 
---------------------------
We can change the position or placement of div. 
In order properties we use either negative value or positive value.


8. flex-basis:
-----------------------------
This property can be usd for to adjust the width of child. 
9. flex-grow
10. align-self
11. flex-shrink
===============================================================================================

display : grid
------------------------------------------
1. flex has one drawback that is it works on only one direction at a time.
2. to solve this we use the grid system. Means display:grid can work on both direction.
3. By using grid system we work at row direction indirectly we increase height as well as we work on column direction indirectly increase the width.
4. For learning the grid we know some properties of grid system.
5. basically we can use the grid system to create image gallery, to create a admin dashboard


The grid system divide as below.
1. Basic grid properties:
----------------------------------
In the basic grid properties below some properties are listed to desgin the page.
1. display:grid 
2. grid-template-columns
3. grid-template-rows
4. grid-column-gap
5. grid-row-gap
6. gap 



2. Placement of grid items/ change the position of grid items
---------------------------------------------------------------
To change the placement of the grid we know the grid number system. below are some properties
1. grid-row-start
2. grid-row-end 
3. grid-column-start
4. grid-column-end
5. grid-row 
6. grid-column 
7. grid-area: grid-row-start / column-start / row-end / column-end



3. Spanning of the grid / merging of grid items
---------------------------------------------------------
To merge row and column we use the following property,
1. grid-row-start
2. grid-row-end 
3. grid-column-start
4. grid-column-end
5. grid-row 
6. grid-column 
7. grid-area: grid-row-start / column-start / row-end / column-end


Explicit Grid Row
Implicit Grid Row 


4. Grid Item align:
    a) align-items
    b) justify-items
    c) place-items
    d) align-self 
    e) justify-self 
    f) place-self

5. Grid Tracking:
    a) align-content
    b) justify-content
    c) place-content

    
6. max-content, min-content and minmax()












































